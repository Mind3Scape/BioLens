/* Справочник показателей: человеческие объяснения.

   Данные собраны по клиническим рекомендациям и справочникам лабораторий —
   источники перечислены в конце файла. Тексты намеренно без диагнозов:
   приложение объясняет, что измеряет показатель и почему он мог сдвинуться,
   а выводы делает врач. */

export const INFO = {
  /* заполняется данными исследования */
};

export function info(key) {
  return INFO[key] || null;
}
